import {Duplex} from "node:stream";import tls from "node:tls";import https from "node:https";export default function diagnosticRequest(connection,hostname,path="/"){const evidence={hostname,method:"GET",dnsAndConnect:false,tls:false,responseReceived:false};return new Promise(resolve=>{const stream=connection.create_stream(hostname,443),bridge=new Duplex({read(){},write(chunk,encoding,done){stream.send(new Uint8Array(chunk));done()},destroy(error,done){if(connection.connected)stream.close();done(error)}});stream.onmessage=data=>bridge.push(Buffer.from(data));stream.onclose=reason=>reason>2?bridge.destroy(Object.assign(new Error("Wisp stream closed"),{code:"WISP_CLOSE_"+reason})):bridge.push(null);const secure=tls.connect({socket:bridge,servername:hostname,rejectUnauthorized:true,ALPNProtocols:["http/1.1"]}),agent=new https.Agent({keepAlive:false});agent.createConnection=()=>secure;let request,finished=false;const finish=error=>{if(finished)return;finished=true;clearTimeout(timer);if(error)evidence.error=error.code||"DIAGNOSTIC_FAILED";request?.destroy();secure.destroy();bridge.destroy();agent.destroy();resolve(evidence)},timer=setTimeout(()=>finish({code:"DIAGNOSTIC_TIMEOUT"}),30000);bridge.on("error",finish);secure.on("error",finish);secure.once("secureConnect",()=>{evidence.dnsAndConnect=true;evidence.tls=secure.authorized;request=https.request({hostname,port:443,path,method:"GET",agent,headers:{Accept:"*/*","Accept-Encoding":"identity",Connection:"close"}},response=>{evidence.status=response.statusCode;evidence.contentType=response.headers["content-type"]||"";evidence.bytes=0;const body=[];response.on("data",chunk=>{evidence.bytes+=chunk.length;if(hostname==="api.ipify.org")body.push(chunk)});response.on("end",()=>{evidence.responseReceived=response.complete;if(hostname==="api.ipify.org")try{evidence.egressIp=JSON.parse(Buffer.concat(body).toString()).ip}catch{evidence.error="IP_ECHO_INVALID"}finish()});response.on("error",finish)});request.on("error",finish);request.end()})})}