import {createServer} from "node:http";import {fileURLToPath} from "node:url";import {dirname,join} from "node:path";import {readFileSync} from "node:fs";import {randomUUID} from "node:crypto";import Fastify from "fastify";import fastifyStatic from "@fastify/static";import {server as wisp,logging} from "@mercuryworkshop/wisp-js/server";import {scramjetPath} from "@mercuryworkshop/scramjet/path";import {libcurlPath} from "@mercuryworkshop/libcurl-transport";import {baremuxPath} from "@mercuryworkshop/bare-mux/node";import residentialRoutes from "./residential.js";import {upstreamPool,getSession,newSession,sessionCookie} from "./upstream-pool.js";import socketForSession from "./wisp-socket.js";import diagnostics,{diagnosticSessions} from "./diagnostics.js";import transportLogs from "./transport-logs.js";
const here=dirname(fileURLToPath(import.meta.url)),publicPath=join(here,"public"),PORT=parseInt(process.env.PORT||"8080",10),installedWisp=JSON.parse(readFileSync(new URL("../../package.json",import.meta.resolve("@mercuryworkshop/wisp-js/server")),"utf8")).version;if(installedWisp!=="0.4.1")throw new Error("Expected wisp-js 0.4.1");if(!process.env.GATEWAY_API_KEY)throw new Error("GATEWAY_API_KEY is required");upstreamPool();logging.set_level(logging.ERROR);Object.assign(wisp.options,{allow_udp_streams:false,allow_private_ips:false,allow_loopback_ips:false,dns_method:"lookup",dns_result_order:"ipv4first"});
const fastify=Fastify({serverFactory:handler=>createServer().on("request",(req,res)=>{res.setHeader("Cross-Origin-Opener-Policy","same-origin");res.setHeader("Cross-Origin-Embedder-Policy","require-corp");res.setHeader("Cross-Origin-Resource-Policy","same-origin");const p=new URL(req.url,"http://gateway.invalid").pathname;if(["/","/proxy.html","/diagnostics"].includes(p)){const session=getSession(req)||newSession(),secure=req.headers["x-forwarded-proto"]==="https"||Boolean(req.socket.encrypted);res.setHeader("Set-Cookie",sessionCookie(session,secure));res.setHeader("Cache-Control","no-store")}if(["/sw.js","/config.js","/proxy.js"].includes(p))res.setHeader("Cache-Control","no-store");if(p==="/sw.js")res.setHeader("Service-Worker-Allowed","/");handler(req,res)}).on("upgrade",(req,socket,head)=>{const diagnostic=diagnosticSessions.get(req.url);if(req.url!=="/wisp/"&&!diagnostic){socket.destroy();return}const session=diagnostic?.session||getSession(req)||newSession(),trace=diagnostic?.trace||{id:randomUUID(),streams:[]};wisp.routeRequest(req,socket,head,{TCPSocket:socketForSession(session,trace)})})});
fastify.register(residentialRoutes);fastify.register(diagnostics);fastify.register(transportLogs);fastify.register(fastifyStatic,{root:publicPath,decorateReply:true,serve:false});for(const[name,file]of[["/","proxy.html"],["/proxy.html","proxy.html"],["/proxy.js","proxy.js"],["/sw.js","sw.js"],["/transport-diagnostics.js","transport-diagnostics.js"]])fastify.get(name,(req,reply)=>reply.sendFile(file));fastify.get("/config.js",(req,reply)=>reply.type("application/javascript").send("self.GHOST_CONFIG = "+JSON.stringify({prefix:"/service/",diagnostics:process.env.WISP_DIAGNOSTICS!=="0"})+";"));fastify.register(fastifyStatic,{root:scramjetPath,prefix:"/scram/",decorateReply:false});fastify.register(fastifyStatic,{root:libcurlPath,prefix:"/libcurl/",decorateReply:false});fastify.register(fastifyStatic,{root:baremuxPath,prefix:"/baremux/",decorateReply:false});fastify.get("/health",async(req,reply)=>{reply.header("access-control-allow-origin","*");reply.header("cache-control","no-store");return{ok:true,build:"scramjet-v6-wisp-connect",wispVersion:installedWisp,transport:"http-connect",directFallback:false,residentialEndpoints:upstreamPool().length}});fastify.listen({port:PORT,host:"0.0.0.0"},err=>{if(err){console.error(err);process.exit(1)}console.log("Ghost Proxy gateway listening on :"+PORT)});