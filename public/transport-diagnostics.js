const requests=new Map();function reportTransport(data){if(self.GHOST_CONFIG?.diagnostics)fetch("/transport-log",{method:"POST",credentials:"same-origin",headers:{"content-type":"application/json"},body:JSON.stringify(data)}).catch(()=>{})}function destinationMeta(url){return{host:url.hostname,port:Number(url.port||(url.protocol==="https:"?443:80))}}function attachTransportDiagnostics(scramjet){scramjet.addEventListener("request",event=>{const meta={...destinationMeta(event.url),method:event.method,id:crypto.randomUUID()};requests.set(event.url.href,meta);reportTransport({...meta,stage:"request"})});scramjet.addEventListener("handleResponse",event=>{const meta=requests.get(event.url.href)||destinationMeta(event.url);requests.delete(event.url.href);reportTransport({...meta,stage:"response",status:event.status,contentType:(event.rawResponse.headers.get("content-type")||"").split(";")[0]})})}