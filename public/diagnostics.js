const out=document.getElementById("results");document.getElementById("run").onclick=async event=>{event.target.disabled=true;out.textContent="Running…";try{const response=await fetch("/diagnostics/run",{method:"POST",credentials:"same-origin"});out.textContent=JSON.stringify(await response.json(),null,2)}catch{out.textContent="FAIL: endpoint unavailable"}finally{event.target.disabled=false}};